<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" />

    <!-- Allow the OpenXR loader to find the runtime through the broker (Android 11+ package visibility) -->
    <queries>
        <provider android:authorities="org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker" />
    </queries>

    <application>
        <!-- PICO OS: mark the application as a VR application -->
        <meta-data android:name="pvr.app.type" android:value="vr" />

        <activity android:name="VPinballActivity"
            android:resizeableActivity="true"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <layout android:defaultHeight="800dp" android:defaultWidth="1200dp" />
        </activity>

        <activity android:name="VPinballPlayerActivity"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Immersive OpenXR session (PICO OS 5+), with the PICO specific category for older runtimes -->
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
                <category android:name="com.picovr.intent.category.VR" />
            </intent-filter>
        </activity>
    </application>

</manifest>
